# MongoDB Connection (You'll need to replace this with MongoDB Atlas URI)
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/library_management?retryWrites=true&w=majority

# Server Configuration
PORT=5000
NODE_ENV=production

# JWT Configuration (Generate a strong secret for production)
JWT_SECRET=your_super_secure_jwt_secret_for_production_change_this
JWT_EXPIRE=7d

# Client Configuration (Will be updated after frontend deployment)
CLIENT_URL=https://your-frontend-app.vercel.app
